/**
 * Single source of truth for the app version shown in the UI.
 * Keep APP_VERSION in sync with the "version" field in package.json.
 */
export const APP_VERSION = "2.5.3";
export const APP_VERSION_LABEL = `v${APP_VERSION} Public Beta`;
